# OCR service — STAGING. Copy to .env.staging (git-ignored) and fill.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-southeast-1

# 1 worker is plenty for staging smoke tests (each worker loads its own model).
WEB_CONCURRENCY=1
